name: Send Daily Email

on:
  schedule:
    # Lundi à vendredi à 09:00 Europe/Paris (08:00 UTC)
    - cron: "0 8 * * 1-5"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  send:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Paris
      EMAIL_FROM: "Scanner <er.gorge@gmail.com>"
      EMAIL_TO: ${{ secrets.EMAIL_RECIPIENTS }}   # Ou mets directement "er.gorge@gmail.com"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ui/requirements.txt || true
          pip install pandas yfinance requests

      - name: Show CLI help (debug)
        run: |
          python scripts/daily_run.py -h || true

      - name: Fetch market data
        run: |
          mkdir -p out
          python scripts/daily_run.py \
            --watchlist data/watchlist.csv \
            --output out/quotes.parquet \
            --period 6mo \
            fetch

      - name: Send email (Gmail SMTP)
        run: |
          python scripts/daily_run.py \
            --watchlist data/watchlist.csv \
            --output out/quotes.parquet \
            --period 6mo \
            email \
            --sender "${EMAIL_FROM}" \
            --recipients "${EMAIL_TO}" \
            --smtp-user "${{ secrets.SMTP_LOGIN }}" \
            --smtp-password "${{ secrets.SMTP_PASSWORD }}"
